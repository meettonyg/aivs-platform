{
  "name": "aivs-platform",
  "private": true,
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "typecheck": "turbo typecheck",
    "test": "turbo test",
    "db:migrate": "turbo db:migrate --filter=@aivs/db",
    "db:generate": "turbo db:generate --filter=@aivs/db",
    "db:studio": "pnpm --filter @aivs/db exec prisma studio",
    "db:promote-admin": "pnpm --filter @aivs/db db:promote-admin",
    "postinstall": "pnpm --filter @aivs/db db:generate"
  },
  "devDependencies": {
    "turbo": "^2.4.0",
    "typescript": "^5.7.0"
  },
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=20.0.0"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "prisma": {
    "schema": "packages/db/prisma/schema.prisma"
  }
}
